/**
 * hello-world.js
 */
function capitalize(word) {
    
    return word.charAt(0).toUpperCase() + word.slice(1);
  }
  
  function hello() {

    return "helloworld";
  }
  
  
  console.log(hello());
  console.log(hello());
  console.log(hello());

